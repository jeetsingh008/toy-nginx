{
  "name": "toy-nginx",
  "version": "1.0.0",
  "description": "Rebuilding nginx's core architecture from scratch in Node.js to understand what it actually does — raw HTTP parsing, reverse proxy, round-robin load balancing, config-driven routing.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "benchmark": "node scripts/benchmark.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
